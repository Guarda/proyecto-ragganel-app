// Interface simple para el tipo de accesorio (ajusta si tienes una más completa)
export interface TipoAccesorio {
  IdTipoAccesorioPK: number;
  CodigoAccesorio: string;
  DescripcionAccesorio: string;
  Activo: boolean;
}
